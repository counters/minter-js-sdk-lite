export default function (id, name, symbol, volume, crr, reserve_balance, max_supply, owner_address) {
    this.id = id;
    this.name = name;
    this.symbol = symbol;
    this.volume = volume;
    this.crr = crr;
    this.reserve_balance = reserve_balance;
    this.max_supply = max_supply;
    this.owner_address = owner_address;
}
